(when true (echo "yes"))
(unless false (echo "unless"))
(cond
  (false (echo "skip"))
  (true (echo "take")))
(-> "nfl" toUpperAscii)
(map [1 2 3] (do ((x int)) (* x 2)))
(progn (echo "a") (echo "b"))
