(defmacro add-one (a)
  `(let ((tmp 1)) (+ tmp ,a)))
(let ((tmp 100)) (add-one tmp))
(defmacro incr-section (a)
  `(block (var ((tmp 1))) (set! tmp (+ tmp ,a)) tmp))
(let ((tmp 100)) (incr-section tmp))
(defmacro mk-adder ()
  `(proc adder ((self int)) (: int) (+ self 1)))
(mk-adder)
