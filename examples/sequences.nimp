(defvar nums (@ [1 2 3 4]))

(defvar doubled
  (map nums (do ((x int64)) (* x 2))))

(defvar small
  (filter nums (do ((x int64)) (< x 4))))

(defvar total
  (foldl nums 0 (do ((acc int64) (x int64)) (+ acc x))))

(echo (first doubled))
(echo (first (rest small)))
(echo total)
